proxs3 (0.7.0-1) unstable; urgency=medium

  * Fix deleted-on-S3 objects being served from cache indefinitely
    (issue #12, thanks Leonorus): HeadObject now distinguishes a 404 from a
    transport error (ErrNotFound), so /v1/download purges the cached copy and
    fails closed when the object is gone instead of treating it as "S3
    unreachable" and serving stale bytes. Matters for mutable, identity-
    bearing artifacts such as cloud-init user-data ISOs.
  * Fix data loss in cache.Link when source path matches the canonical cache
    path (regression hit on /v1/upload of files already living in cache):
    the copy fallback would os.Create the destination before reading from
    the source, truncating the file to zero bytes; the watcher would then
    upload the now-empty file to S3, clobbering the just-uploaded copy.
  * Fix cache self-eviction loop: a single file larger than cache_max_mb
    was deleted moments after being downloaded, which made PVE restores of
    multi-GB backups loop forever (download -> evict -> retry). Files
    modified within the last 30 minutes are now exempt from size-based
    eviction; a warning is logged when the cache stays over budget.
  * Fix /v1/download destroying locally created files not yet uploaded to
    S3: a HeadObject 404 was treated as "deleted on S3" and purged the
    cached file even when it had never been uploaded (e.g. cloud-init
    user-data ISOs written by PVE and attached immediately, inside the
    watcher's debounce window), losing the file permanently. Files are now
    explicitly tracked as pending upload: the watcher writes a durable
    pending-upload marker when it detects a local write, and while a file
    is pending (or has no metadata at all) download serves the local copy
    and queues the upload instead of purging it or overwriting it with a
    stale S3 version of the same key. Files confirmed synced still fail
    closed on 404. Pending files survive restarts (the watcher scans the
    cache for unconfirmed files on startup) and are exempt from cache
    eviction until uploaded. An upload trigger arriving mid-transfer now
    reruns the upload instead of being dropped, delete aborts any in-flight
    upload of the same file so it can't resurrect the object, and uploads
    record the S3 ETag so the next download doesn't needlessly re-fetch.
  * Fix PVE restore "connection error timeout" in the UI: path() no longer
    triggers a synchronous download from the API thread for content types
    where activate_volume is called (backup, iso, vztmpl, images). PVE's
    worker task handles the fetch via activate_volume where there is no
    pveproxy timeout. Snippets and import keep the prior behavior.
  * Switch /v1/download to AWS SDK manager.Downloader (4 x 64MB chunks,
    parallel Range GETs with per-chunk retry) — large-object downloads
    now survive transient network failures instead of restarting from
    zero. Symmetric with the existing manager.Uploader path.
  * Add explicit S3 HTTP transport timeouts (DialContext 30s, TLS 15s,
    ResponseHeader 60s) — fast-fail on connection problems while keeping
    body transfers unbounded.
  * Download via .downloads/<storage>/ tmp tree (sibling of the storage
    cache, outside the watcher's view), then atomic rename into place.
    Stops the watcher from ever seeing partial files.
  * Log download progress every 30 seconds with throughput so long
    transfers leave a useful trail in journalctl.
  * Bump HEAD timeout in /v1/download from 10s to 60s to handle slow S3
    regions without spurious "S3 unreachable" warnings.
  * Add `proxs3d --resync STORAGE` (with optional --force-latest): walks
    the local cache for one storage and uploads anything missing in S3
    or where the local copy is newer than the remote object. Streams
    progress to stdout; talks to the running daemon over the Unix socket.
  * Report the running PVE storage API version from api() instead of a
    hardcoded 13. PVE 9.2 is APIVER 14, so the old value triggered an
    "implementing an older storage API" warning on every plugin load and
    would have hard-failed once APIVER-APIAGE rose above 13. api() now
    returns the live APIVER when it falls within the validated 11..14 range
    (clamping to 14 otherwise), so the plugin tracks PVE forward without
    code changes. No new methods required: APIVER 14 adds only the optional
    get_identity() (N/A for an object store) and 13's activate_volume $hints
    arg is already accepted.

 -- Dave Kempe <dave@sol1.com.au>  Thu, 18 Jun 2026 21:19:42 +1000

proxs3 (0.6.5-1) unstable; urgency=medium

  * Fix delete leaving stale local cache files that block re-upload (issue #11)
  * Make chattr immutable-flag removal more robust in systemd environments
  * Log errors when local cache removal fails during delete

 -- Dave Kempe <dave@sol1.com.au>  Wed, 09 Apr 2026 12:00:00 +1000

proxs3 (0.6.4-1) unstable; urgency=medium

  * Fix rename/copy when source not yet in S3

 -- Dave Kempe <dave@sol1.com.au>  Wed, 09 Apr 2026 09:00:00 +1000

proxs3 (0.6.3-1) unstable; urgency=medium

  * Add --version flag to proxs3d and fix README wget URL

 -- Dave Kempe <dave@sol1.com.au>  Tue, 08 Apr 2026 09:00:00 +1000

proxs3 (0.6.2-1) unstable; urgency=medium

  * Merge local cache files into /v1/list for immediate visibility (PR #8)
  * Skip .meta directory in mergeLocalFiles for images content

 -- Dave Kempe <dave@sol1.com.au>  Mon, 07 Apr 2026 09:00:00 +1000

proxs3 (0.6.1-1) unstable; urgency=medium

  * Fix import-from: download file to cache when not present (PR #6, issue #4)
  * Add configurable part-size-mb for MinIO compatibility (PR #5, issue #3)

 -- Dave Kempe <dave@sol1.com.au>  Wed, 25 Mar 2026 09:51:00 +1100

proxs3 (0.6.0-1) unstable; urgency=medium

  * Implement backup protected attribute via S3 object tags
  * Add images content type to UI content type selector
  * Complete PVE storage plugin API audit — all items resolved

 -- Dave Kempe <dave@sol1.com.au>  Tue, 24 Mar 2026 16:43:00 +1100

proxs3 (0.5.2-1) unstable; urgency=medium

  * Fix duplicate volume names on rapid consecutive allocations

 -- Dave Kempe <dave@sol1.com.au>  Sun, 22 Mar 2026 20:35:00 +1100

proxs3 (0.5.1-1) unstable; urgency=medium

  * Fix volume_size_info to match file_size_info return convention

 -- Dave Kempe <dave@sol1.com.au>  Sun, 22 Mar 2026 16:33:00 +1100

proxs3 (0.5.0-1) unstable; urgency=medium

  * Implement S3-native template conversion (create_base), clone, and rename
  * Add /v1/copy and /v1/rename daemon endpoints (S3 CopyObject)
  * Override volume_has_feature to accurately report S3 capabilities
  * Override list_images to query S3 instead of local cache (fixes name collisions)
  * Override volume_size_info with cache-first, S3 metadata fallback
  * Backup pruning removes from local cache only — S3 backups preserved
  * Add check_connection to detect daemon availability
  * Explicitly disable volume_resize on S3 storage

 -- Dave Kempe <dave@sol1.com.au>  Sun, 22 Mar 2026 16:03:00 +1100

proxs3 (0.4.5-1) unstable; urgency=medium

  * Include vmid in list_volumes for images content type (fixes UI delete)

 -- Dave Kempe <dave@sol1.com.au>  Sun, 22 Mar 2026 15:12:00 +1100

proxs3 (0.4.4-1) unstable; urgency=medium

  * Separate path resolution from download — /v1/path no longer triggers S3 download
  * Handle PVE immutable flag (chattr +i) on template base images in cache
  * Clear immutable flag during cache removal, age eviction, and LRU eviction

 -- Dave Kempe <dave@sol1.com.au>  Sun, 22 Mar 2026 15:04:00 +1100

proxs3 (0.4.3-1) unstable; urgency=medium

  * Fix alloc_image to create disk file with qemu-img create
  * Fix watcher to monitor vmid subdirectories under images/

 -- Dave Kempe <dave@sol1.com.au>  Sun, 22 Mar 2026 13:11:00 +1100

proxs3 (0.4.2-1) unstable; urgency=medium

  * Register as shared storage — all nodes see the same S3 bucket

 -- Dave Kempe <dave@sol1.com.au>  Sun, 22 Mar 2026 09:57:00 +1100

proxs3 (0.4.1-1) unstable; urgency=medium

  * Implement alloc_image for disk clone support

 -- Dave Kempe <dave@sol1.com.au>  Sat, 21 Mar 2026 12:02:00 +1100

proxs3 (0.4.0-1) unstable; urgency=medium

  * Add images content type support for VM disk templates
  * Handle vmid/diskname volname format for images content
  * Add images prefix mapping to daemon, watcher, and cache directory creation

 -- Dave Kempe <dave@sol1.com.au>  Sat, 21 Mar 2026 11:50:00 +1100

proxs3 (0.3.3-1) unstable; urgency=medium

  * Override parse_volname to untaint all path components for PVE taint mode

 -- Dave Kempe <dave@sol1.com.au>  Sat, 21 Mar 2026 09:11:00 +1100

proxs3 (0.3.2-1) unstable; urgency=medium

  * Fix taint-mode error in check_config for qemu-img import path

 -- Dave Kempe <dave@sol1.com.au>  Sat, 21 Mar 2026 09:05:00 +1100

proxs3 (0.3.1-1) unstable; urgency=medium

  * Fix taint-mode error in path() when PVE calls qemu-img

 -- Dave Kempe <dave@sol1.com.au>  Sat, 21 Mar 2026 08:53:00 +1100

proxs3 (0.3.0-1) unstable; urgency=medium

  * Fix import content type: align S3/cache prefix with PVE expected layout
  * Fix path() return signature to match PVE ($path, $vmid, $vtype) convention
  * Fix watcher and cache directory creation for import subdirectory

 -- Dave Kempe <dave@sol1.com.au>  Sat, 21 Mar 2026 08:30:00 +1100

proxs3 (0.2.4-1) unstable; urgency=medium

  * Fix taint-mode error in activate_storage when creating cache directories

 -- Dave Kempe <dave@sol1.com.au>  Wed, 18 Mar 2026 12:00:00 +0000

proxs3 (0.2.3-1) unstable; urgency=medium

  * Add stable-named deb (proxs3_amd64.deb) to release pipeline

 -- Dave Kempe <dave@sol1.com.au>  Sun, 09 Mar 2026 15:00:00 +0000

proxs3 (0.2.2-1) unstable; urgency=medium

  * Show S3 bucket@endpoint in PVE Path/Target column instead of cache path
  * Fix cache_dir query to run safely at activation time, not in polling loop

 -- Dave Kempe <dave@sol1.com.au>  Sun, 09 Mar 2026 14:00:00 +0000

proxs3 (0.2.1-1) unstable; urgency=medium

  * Fix cache_dir config being ignored - plugin now queries daemon for
    the actual cache path instead of hardcoding /var/cache/proxs3 (#1)
  * Remove CacheDirectory= from systemd unit (was breaking symlinks)
  * Fix build on Debian 12 Bookworm build environments (#2)
  * Require PVE 9+ at runtime (plugin API targets PVE 9)
  * Add /v1/config API endpoint
  * Comprehensive test suite (95 tests) with S3 client mock interface

 -- Dave Kempe <dave@sol1.com.au>  Sun, 09 Mar 2026 10:00:00 +0000

proxs3 (0.2.0-1) unstable; urgency=medium

  * Multipart upload support for files >5GB (AWS SDK upload manager)
  * Prevent uploading partial writes (fileInUse /proc/fd check, skip .tmp files)
  * Per-storage cache-max-age property for automatic local cache eviction
  * Cache metadata in .meta/ subdirectory (fixes watcher sidecar loop)
  * activate_volume downloads S3 objects on demand
  * Graceful degradation when S3 is unreachable (timeouts, stale cache serving)
  * Endpoint config sanitisation (strip protocol prefix and trailing slash)
  * Credentials optional for public buckets

 -- Dave Kempe <dave@sol1.com.au>  Sun, 08 Mar 2026 17:00:00 +0000

proxs3 (0.1.0-1) unstable; urgency=medium

  * Initial release
  * Go daemon (proxs3d) with S3 client, local cache, health monitoring
  * Perl PVE storage plugin for iso, vztmpl, snippets, backup
  * systemd service unit

 -- Dave Kempe <dave@sol1.com.au>  Fri, 07 Mar 2026 17:00:00 +0000
